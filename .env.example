# ===== Interview Forge 环境变量示例 =====
# 复制本文件为 .env 并填入真实值：cp .env.example .env
# 注意：.env 含密钥，已被 .gitignore 忽略，绝不会进版本库。

# 必填 · Prisma 唯一数据源（PostgreSQL 连接串）
# 可用 Neon / Supabase / Railway 等任意 Postgres 实例
# 例：postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech:5432/neondb?sslmode=require
#
# ⚠️ 重要：请为「本展示仓」创建一个【独立的 Neon 项目 / 数据库】，
#    不要复用你真实生产环境的 DATABASE_URL。
#    —— 展示仓会建表、写入演示数据，连真实库会污染你的真实题库，
#       也违背「公开 demo 不连真实数据」的初衷。
DATABASE_URL="postgresql://user:pass@host:5432/db?sslmode=require"

# 本地可选 / 部署时不要设 · DeepSeek 大模型密钥
# 获取：https://platform.deepseek.com/api_keys
#
# Key 的取值优先级（见 src/lib/ai/client.ts 的 getApiKey）：
#   ① 访客在 /ai-generate 页面填写、经 AES 加密存入 httpOnly Cookie 的 Key（生产环境走这条）
#   ② 本文件的 DEEPSEEK_API_KEY（本地开发的兜底）
#   ③ 都没有 → 报错提示填写
#
# - 【本地开发】可留空：不填也能跑完整流程，在页面填一次自己的 Key 即可
# - 【部署 Vercel】❌ 不要设：设了会退化成「所有访客共用部署者 Key」，消耗你的额度。
#   正确做法是让每位访客自带 Key（详见 README「部署」一节）
DEEPSEEK_API_KEY="sk-..."

# 必填（只要启用 AI 出题）· 用户自备 API Key 的加密密钥（AES-256-GCM）
# 页面填 Key 时由 /api/ai/setup-key 加密、调 AI 时由 getApiKey 解密；
# 不设则「填写 Key」这一步会直接抛错（crypto.ts 会提示环境变量未设置）
# 自行生成 32 字节的十六进制串：openssl rand -hex 32
API_KEY_ENCRYPTION_SECRET="此处填入 32 字节 hex（openssl rand -hex 32 生成）"

# 可选 · 全站密码门（SITE_PASSWORD）
# 留空 = 全站开放（默认）；填入任意字符串则开启全站访问守卫（/unlock 输入密码）
SITE_PASSWORD=""

# ----- 以下变量代码不读取，请勿填入（历史/环境残留）-----
# HTTPS_PROXY / HTTP_PROXY   → 仅本地沙箱开发绕行用，部署无需
# PG* / POSTGRES_*           → Neon 顺带给出的冗余变量，Prisma 只读 DATABASE_URL
